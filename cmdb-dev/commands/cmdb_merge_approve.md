---
description: Explicitly approve a high-risk CMDB PR merge, then stop for the human tag/image confirmation gate.
argument-hint: "<REQ-123 or BUG-123>"
skills: cmdb-development
---
The user explicitly authorizes merge for $ARGUMENTS. This command covers high-risk merges under either merge guard. Call `cmdb_status` with GitHub refresh when resuming across sessions or when local state is stale or suspect (a `cmdb_verify_pr_checks` performed in this session is revision-guarded and fresh), and verify waiting_human_merge, passed Tester/Reviewer/workflow checks, persisted `pr_head_sha`, a verified merge guard, and no blocker. Call `cmdb_transition(approve_merge, actor=human:<identity>)` to persist Gate B; approval never bypasses checks. Perform exactly one repository-compatible merge command that includes `--match-head-commit <persisted-pr-head-sha>` — no token is needed, the guard state-verifies merges from the `merging` state. Verify merged SHA, call `cmdb_transition(pr_merged)` with that SHA, then split by the persisted delivery policy: when delivery_required is false and skip_allowed is true, record `policy_skip` citing the Gate A approval, close the exact Issue once (state-verified, no token), record issue_closed, housekeep the merged worktree — plain `git worktree remove <worktree_path>` (never `--force`) then `git branch -d <branch>` from the managed repository — and report Done; otherwise STOP at waiting_tag_confirm. Never invoke the legacy state CLI.
