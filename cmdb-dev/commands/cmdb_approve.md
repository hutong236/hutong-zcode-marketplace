---
description: Approve a CMDB Work Item and run the automated pipeline through code, test, review, PR, merge, tag confirmation, image verification, and Done.
argument-hint: "<REQ-123 or BUG-123> [optional constraints]"
skills: cmdb-development
---
The user explicitly approves: $ARGUMENTS. Act as Primary Agent Orchestrator and use `cmdb-control` MCP tools for every state/authority action. Call `cmdb_status` with GitHub refresh, then `cmdb_transition(approve_requirement, actor=human:<identity>)` to record Gate A. Resolve and fetch the current default branch, then call `cmdb_worktree_create` with explicit base `origin/<default-branch>`; pin Planner completion, Coder, Tester, and Reviewer to the returned path. Record every milestone with `cmdb_transition`; never hand-edit state. Stop when the three-round rework budget blocks. After reviewer approval commit only related files, call `cmdb_authorize(git-push)` and use the returned token for exactly one push from the recorded worktree cwd. Create a PR with `Refs #issue`, record `pr_created`, then call `cmdb_verify_pr_checks`; never submit `checks_passed` through the generic transition tool. Public repositories require GitHub-enforced checks. Private repositories without paid branch protection use the exact-SHA control-plane guard and always stop at waiting_human_merge. GitHub-enforced high risk also stops there; only GitHub-enforced low/medium risk may auto-merge. Every authorized merge command must include `--match-head-commit <persisted-pr-head-sha>`. Verify merged SHA, record pr_merged, and STOP at waiting_tag_confirm. Do not invoke the legacy state CLI or bypass MCP.
