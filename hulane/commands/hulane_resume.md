---
description: Resume an interrupted Hulane AI development Work Item from verified Git/GitHub state instead of chat memory.
argument-hint: "<REQ-123 or BUG-123>"
skills: hulane-development
---
Resume $ARGUMENTS as Primary Agent Orchestrator. Do not trust chat memory. Call `hulane_status` with GitHub refresh and `hulane_validate`, then use the returned canonical revision to regenerate cache/projection. Verify branch/worktree before dispatch. Follow exactly one next state event: waiting_approval stops; ready creates worktree; doing/testing/review dispatch their pinned agents (testing and review in parallel after a Coder completes); pr_checking first runs one `gh run list` query for the PR branch — a concluded run goes straight to `hulane_verify_pr_checks`, a still-running one re-arms one background `gh run watch <run-id> --exit-status --interval 30` Bash task and ends the turn (never block or poll in the foreground); waiting_human_merge stops for humans; waiting_tag_confirm stops for humans only when the item must tag (delivery_required true) — a skip-allowed item sitting there (session died between merge and close) instead records `policy_skip` citing the Gate A approval, closes the Issue, and reports Done; building dispatches Build Checker; waiting_close performs one state-verified Issue close (no token); after any resume-driven issue_closed, housekeep the merged worktree — plain `git worktree remove <worktree_path>` (never `--force`) then `git branch -d <branch>` from the managed repository; blocked requires resolution evidence. Push and tag operations use a fresh `hulane_authorize` token; merges and Issue closes are state-verified by the guard without tokens, and every merge pins the persisted PR Head SHA. Never infer a Gate or invoke the legacy state CLI.
